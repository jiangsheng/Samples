This Windows program automates Microsoft Outlook to get the number of emails from each sender address and save as a CSV file. 

It then tries to open the file, by default it would be opened in Microsoft Excel. 

Requires Microsoft Outlook and .Net 8.0. 
